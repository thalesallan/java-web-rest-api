# Workflow para criação automática de releases baseado em Conventional Commits
name: 🚀 Release Automático

on:
  push:
    branches: [ main ]

jobs:
  release:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, 'chore(release)') && !contains(github.event.head_commit.message, '[skip ci]')"
    permissions:
      contents: write
      issues: write
      pull-requests: write
    
    steps:
    - name: 📥 Baixar código
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: ☕ Configurar Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 📦 Instalar semantic-release
      run: npm install semantic-release @semantic-release/github

    - name: 🔧 Criar configuração do semantic-release
      run: |
        cat > .releaserc.json << 'EOF'
        {
          "branches": ["main"],
          "repositoryUrl": "https://github.com/${{ github.repository }}",
          "plugins": [
            "@semantic-release/commit-analyzer",
            "@semantic-release/release-notes-generator",
            "@semantic-release/github"
          ]
        }
        EOF    - name: 🔍 Verificar commits para release
      id: check
      run: |
        # Verifica se há commits desde a última tag que justifiquem uma release
        LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
        if [ -z "$LAST_TAG" ]; then
          echo "Nenhuma tag anterior encontrada, tentará criar release"
          echo "should_release=true" >> $GITHUB_OUTPUT
        else
          # Verifica commits desde a última tag
          COMMITS=$(git log $LAST_TAG..HEAD --oneline --grep="^feat\|^fix\|^perf\|BREAKING CHANGE" || echo "")
          if [ -n "$COMMITS" ]; then
            echo "Commits encontrados para release:"
            echo "$COMMITS"
            echo "should_release=true" >> $GITHUB_OUTPUT
          else
            echo "Nenhum commit encontrado para release"
            echo "should_release=false" >> $GITHUB_OUTPUT
          fi
        fi

    - name: 🏷️ Criar release automática
      if: steps.check.outputs.should_release == 'true'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: npx semantic-release

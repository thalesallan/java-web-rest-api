# Workflow único otimizado - CI/CD + Deploy Railway
name: 🚀 CI/CD & Deploy

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  # Job 1: Build e Testes (sempre executa)
  test-and-build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
    
    - name: ☕ Setup JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: 🧪 Executar testes
      run: mvn -B test

    - name: 🔨 Build da aplicação
      run: mvn -B clean package -DskipTests

    - name: ✅ Verificar integridade
      run: mvn -B verify -DskipTests

    - name: 📦 Upload JAR artifact
      uses: actions/upload-artifact@v4
      with:
        name: app-jar
        path: target/*.jar
        retention-days: 7

    - name: 🎯 Status do build
      run: |
        echo "✅ Build concluído com sucesso!"
        echo "📦 JAR criado: $(ls target/*.jar)"
        echo "🚂 Pronto para Railway!"
        echo ""
        echo "🔗 Para deploy no Railway:"
        echo "1. Acesse: https://railway.app"
        echo "2. New Project → Deploy from GitHub repo"
        echo "3. Selecione este repositório"
        echo "4. Deploy automático!"

  # Job 2: Release automática (apenas push para main com commits relevantes)
  auto-release:
    runs-on: ubuntu-latest
    needs: test-and-build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: ☕ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: 📦 Setup semantic-release
      run: |
        npm install semantic-release @semantic-release/github
        cat > .releaserc.json << 'EOF'
        {
          "branches": ["main"],
          "repositoryUrl": "https://github.com/${{ github.repository }}",
          "plugins": [
            "@semantic-release/commit-analyzer",
            "@semantic-release/release-notes-generator",
            "@semantic-release/github"
          ]
        }
        EOF

    - name: 🏷️ Criar release (se necessário)
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: npx semantic-release

    - name: ☕ Setup JDK 17 (para publish)
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        server-id: github
        settings-path: ${{ github.workspace }}

    - name: 📥 Download JAR artifact
      uses: actions/download-artifact@v4
      with:
        name: app-jar
        path: target/

    - name: 📦 Publicar no GitHub Packages (se release foi criada)
      run: |
        if git describe --tags --exact-match HEAD >/dev/null 2>&1; then
          echo "🏷️ Tag encontrada, publicando no GitHub Packages..."
          mvn deploy -s $GITHUB_WORKSPACE/settings.xml -DskipTests
        else
          echo "ℹ️ Nenhuma tag criada, pulando publicação"
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
